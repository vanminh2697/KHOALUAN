# def find(words):
#     for i in 

def pre():
    string = "   nguyen van minh    "
    # handing double 
    lists = string.split(" ")
    print(lists)
    strings = []
    for i in lists:
        if i != "":
            strings.append(i)
    s = " ".join(strings)
    print(s)
    # handing double !!!
    # stop = ['.', ',', '"', "'", '?', '!', ':', ';', '(', ')', '[', ']', '{', '}', '@', '#', '$', '%', '^', '&', '*', '+', '=', '\\', '|', '`', '~']
    
pre()